// an example proto "schema" file.

syntax = "proto3";

message Employee {
    int32 id = 1;
    string name = 2;
    float salary = 3;
}

// repeated is an array
message Employees {
    repeated Employee employees = 1;
}